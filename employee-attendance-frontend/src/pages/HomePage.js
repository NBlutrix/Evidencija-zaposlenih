import React from 'react';

const HomePage = () => (
  <div>
    <h1>Welcome to Employee Attendance App</h1>
    <p>Use the navigation bar to manage attendances or export ICS files.</p>
  </div>
);

export default HomePage;